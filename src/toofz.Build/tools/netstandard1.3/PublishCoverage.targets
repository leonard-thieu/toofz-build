<Project>
  <UsingTask AssemblyFile="$(BuildTasksAssemblyPath)" TaskName="toofz.Build.Codecov" />

  <Target Name="PublishCoverage">
    <Codecov ToolPath="$(CodecovToolPath)"
             File="$(CodeCoverageResultsPath)" />
  </Target>
</Project>