<Project>

  <PropertyGroup>
    <ToolsDir>$(MSBuildThisFileDirectory)..\..\tools\</ToolsDir>
  </PropertyGroup>

  <Import Project="$(ToolsDir)Common.targets" />
  <Import Project="$(ToolsDir)TestProject.targets" />
  <Import Project="$(ToolsDir)SubmitCodeCoverage.targets" />
  <Import Project="$(ToolsDir)DeployNuGetPackage.targets" />

</Project>